<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="<%= basePath %>/main.css">
</head>
<body>
  <%- include('partials/nav') %>
  <h1>Login</h1>
  <% if (message) { %>
    <div class="<%= message.includes('success') ? 'success' : (message.includes('Invalid') ? 'error' : 'info') %>">
      <%= message %>
    </div>
  <% } %>
  <form method="POST" action="<%= basePath %>/loggedin">
    <label>Username</label>
    <input type="text" name="username" required placeholder="Enter your username" autofocus>
    
    <label>Password</label>
    <input type="password" name="password" required placeholder="Enter your password">
    
    <button type="submit">Login</button>
  </form>
  
  <p style="text-align: center; margin-top: 1rem;">
    Don't have an account?
    <a href="<%= basePath %>/register" style="color: #3498db;">Create one here</a>
  </p>
</body>
</html>